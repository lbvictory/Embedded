### 第1周：工程化重构与软件定时器（不需要重新学，只需整理）

**目标**：把你现有的零散代码变成可维护的工程。

- 🔴 **核心任务**（每天1小时）：
  
  - 周一：创建Git仓库，提交现有代码（哪怕乱，先提交）
  
  - 周二：把GPIO、UART、OLED、DHT11分别拆成独立`.c/.h`，放到`Hardware/`文件夹
  
  - 周三：统一命名风格（如`dht11_init()`, `dht11_read()`），添加函数头注释
  
  - 周四：实现软件定时器框架（基于SysTick，10ms心跳）📝寄存器（直接操作SysTick->LOAD和->VAL）
  
  - 周五：用软件定时器让LED以3Hz闪烁，同时串口每秒打印一次“Alive”
  
  - 周六～周日：缓冲（若未完成则补做）

- 🟡 **拓展任务**：写一篇“工程化重构笔记”，列出你做了哪些改进

**里程碑**：你的代码可以给别人看，且能通过串口看到稳定的心跳打印。

---

### 第2-3周：SPI驱动开发（📝寄存器为主）

**目标**：用寄存器方式实现SPI通信，并用逻辑分析仪验证时序。

**第2周：SPI基础 + 发送数据**

- 🔴 **核心任务**：
  
  - 周一：阅读STM32F103参考手册SPI章节，理解NSS、SCK、MOSI、MISO
  
  - 周二：配置SPI1的GPIO（PA5-SCK, PA7-MOSI, PA6-MISO）📝寄存器（GPIO_CRL, AFIO）
  
  - 周三：初始化SPI1为Master，模式0，8位，速率4分频📝寄存器（SPI1_CR1, CR2, SR）
  
  - 周四：写`spi_send_byte(uint8_t data)`，用逻辑分析仪抓波形
  
  - 周五：循环发送0x55，观察SCK和MOSI是否匹配
  
  - 周六～周日：缓冲

- 🟡 **拓展**：用SPI驱动一个74HC595（输出扩展）或简单的SPI Flash读ID

**第3周：SPI读写 + 驱动一个真实模块**

- 🔴 **核心任务**：
  
  - 周一：实现`spi_read_write(uint8_t out)`，同时读写
  
  - 周二：购买或使用已有SPI模块（推荐W25Q64 Flash模块，淘宝10元）
  
  - 周三：读W25Q64的JEDEC ID（指令0x9F），串口打印ID
  
  - 周四：实现页写和读数据（指令0x02和0x03），验证读写一致
  
  - 周五：用逻辑分析仪抓一整个命令序列，标注每个阶段
  
  - 周六～周日：缓冲

- 🟡 **拓展**：实现Flash的擦除功能

**里程碑**：你能通过SPI读写外部Flash，并能解释每个时钟沿的数据传输。

---

### 第4周：DMA + 输入捕获（🔧HAL库配置底层，理解寄存器）

**目标**：用DMA提高ADC采集效率，用输入捕获测量PWM周期。

**第4周（DMA部分前3天，输入捕获后2天）**

- 🔴 **核心任务**：
  
  - 周一：用HAL配置ADC1单通道，软件触发，用DMA搬运结果🔧HAL（`HAL_ADC_Start_DMA`）
  
  - 周二：测量一个电位器的电压，每100ms打印一次，观察CPU占用率（与不用DMA对比）
  
  - 周三：改成多通道扫描模式+DMA，采集3路模拟量
  
  - 周四：配置定时器2的通道1为输入捕获模式🔧HAL + 寄存器（捕获极性、分频）
  
  - 周五：产生一个PWM信号（用定时器3），用输入捕获测量其频率和占空比，串口打印
  
  - 周六～周日：缓冲

- 🟡 **拓展**：用DMA+UART实现环形缓冲区接收不定长数据

**里程碑**：你能用DMA高效采集ADC，并用输入捕获测量外部PWM信号。

---

### 第5-6周：FreeRTOS移植与任务管理（🔧HAL + 寄存器配置中断）

**目标**：移植FreeRTOS，创建任务，理解调度。

**第5周：移植 + 创建任务**

- 🔴 **核心任务**：
  
  - 周一：下载FreeRTOS源码，移植到你的工程（参考野火/正点原子教程）
  
  - 周二：配置SysTick为FreeRTOS心跳，修改中断优先级分组⚡（寄存器设置NVIC，确保HAL库中断优先级高于FreeRTOS）
  
  - 周三：创建两个任务：Task_LED（闪烁）和Task_Print（串口打印计数值）
  
  - 周四：设置不同优先级，观察抢占行为（用逻辑分析仪抓GPIO翻转）
  
  - 周五：使用`vTaskDelayUntil`实现精确1秒周期任务
  
  - 周六～周日：缓冲

- 🟡 **拓展**：用`uxTaskGetStackHighWaterMark`检测栈使用量

**第6周：队列、信号量、互斥锁**

- 🔴 **核心任务**：
  
  - 周一：创建队列，Task_ADC采集数据后发送，Task_OLED接收并显示
  
  - 周二：用二进制信号量同步中断（比如按键中断释放信号量，任务打印“Key Pressed”）
  
  - 周三：创建两个任务共享串口打印，不用互斥锁制造冲突，然后用互斥锁解决
  
  - 周四：模拟优先级反转（三个任务：高、中、低，共享互斥锁），观察现象并解释
  
  - 周五：将之前的软件定时器改为FreeRTOS软件定时器（`xTimerCreate`）
  
  - 周六～周日：缓冲

- 🟡 **拓展**：实现任务通知代替队列（精简通信）

**里程碑**：你能画出自己工程的任务通信图，并能解释优先级反转的解决方案。

---

### 第7-8周：巩固RTOS + 将现有裸机项目改造成RTOS版

**目标**：把第1周重构后的DHT11+OLED+串口项目，用RTOS重新实现。

**第7周：任务拆分与移植**

- 🔴 **核心任务**：
  
  - 周一～周二：把DHT11读取任务化（每2秒采集一次，通过队列发送温湿度结构体）
  
  - 周三：OLED显示任务（阻塞接收队列，更新显示）
  
  - 周四：按键扫描任务（状态机，通过队列发送按键事件）
  
  - 周五：串口上报任务（每5秒发送一次当前温湿度）
  
  - 周六～周日：缓冲

- 🟡 **拓展**：增加看门狗任务（独立看门狗，定时喂狗）

**第8周：调试与优化**

- 🔴 **核心任务**：
  
  - 周一～周二：解决任务间共享OLED的互斥问题（用互斥锁或队列）
  
  - 周三：调整任务优先级，确保采集任务不被显示任务阻塞
  
  - 周四：测试长时间运行（至少4小时），记录有无死机
  
  - 周五：编写任务说明文档（优先级、栈大小、通信方式）
  
  - 周六～周日：缓冲

- 🟡 **拓展**：使用Tracealyzer或类似工具可视化任务调度

**里程碑**：你的RTOS版环境监测仪能稳定运行24小时不卡死。

---

### 第9-10周：RS485硬件通信（🔧HAL + 寄存器控制方向脚）

**目标**：实现两个STM32板子之间的RS485通信。

**第9周：RS485基础**

- 🔴 **核心任务**：
  
  - 周一：购买MAX485模块（两个）和双绞线，连接STM32的USART2
  
  - 周二：配置USART2为115200, 8N1，用GPIO控制RE/DE脚⚡（寄存器写BSRR/BRR）
  
  - 周三：写`rs485_send_byte()`和`rs485_receive_byte()`（收发前切换方向）
  
  - 周四：两个板子互发简单字符（如板A发0x55，板B收到后回传0xAA）
  
  - 周五：用逻辑分析仪抓RS485差分信号（A/B线），验证波形
  
  - 周六～周日：缓冲

- 🟡 **拓展**：增加CRC8校验，处理总线冲突

**第10周：RS485多节点通信（简易协议）**

- 🔴 **核心任务**：
  
  - 周一：设计简单主从协议（主机发送“地址+命令”，从机应答）
  
  - 周二：三个板子：一个主机，两个从机（地址1和2）
  
  - 周三：主机轮询从机，从机返回自己的累加计数器
  
  - 周四：处理从机无应答超时（用软件定时器）
  
  - 周五：压力测试（1000次轮询，统计成功率）
  
  - 周六～周日：缓冲

- 🟡 **拓展**：增加广播命令（地址0xFF）

**里程碑**：你有一个RS485主从网络，能稳定通信。

---

### 第11-12周：Modbus RTU协议（🔧HAL + 寄存器CRC计算）

**目标**：实现Modbus RTU从机，支持功能码03和06。

**第11周：Modbus帧解析与CRC**

- 🔴 **核心任务**：
  
  - 周一：学习Modbus RTU帧格式（地址+功能码+数据+CRC），计算CRC16（查表法）📝寄存器（直接用C实现）
  
  - 周二：写`modbus_crc`函数，验证正确性（对比Modbus Poll工具计算值）
  
  - 周三：实现帧接收状态机（空闲→接收→完成），检测3.5字符超时⚡（用定时器）
  
  - 周四：解析收到的帧，分离地址、功能码、数据
  
  - 周五：实现异常响应（如功能码不支持返回0x81）
  
  - 周六～周日：缓冲

- 🟡 **拓展**：实现功能码01（读线圈）作为练习

**第12周：实现保持寄存器读写（03/06）**

- 🔴 **核心任务**：
  
  - 周一：定义保持寄存器数组（如`uint16_t holding_reg[10]`）
  
  - 周二：实现功能码03（读保持寄存器）：把指定地址的寄存器数据打包发送
  
  - 周三：实现功能码06（写单个寄存器）：修改寄存器值并返回相同帧
  
  - 周四：把DHT11的温度和湿度存入寄存器（如温度存到寄存器0，湿度存到寄存器1）
  
  - 周五：用Modbus Poll软件连接STM32，读写寄存器，验证正确性
  
  - 周六～周日：缓冲

- 🟡 **拓展**：实现功能码16（写多个寄存器）

**里程碑**：你可以用Modbus Poll软件读取STM32上的温湿度数据，并远程修改报警阈值。

---

### 第13周：复习周（不学新内容）

**任务**：

- 整理所有代码，删除冗余，统一注释风格

- 更新Git仓库，写详细的README（硬件连接、编译方法、功能说明）

- 录制一个3分钟演示视频（展示Modbus读写、OLED显示、RTOS任务运行）

- 休息，回顾前12周的学习笔记，标记易错点

**产出**：一个完整的、可交付的GitHub项目。

---

### 第14-16周：综合项目——工业数据采集终端

**目标**：整合所有技能，做出一个可演示的完整产品原型。

**功能要求**：

1. 采集：DHT11（温湿度）+ ADC（电位器模拟工业4-20mA传感器）

2. 显示：OLED显示温湿度、ADC值、Modbus寄存器内容

3. 交互：按键切换显示界面（使用状态机）

4. 通信：RS485 + Modbus RTU从机，支持03/06功能码

5. 系统：FreeRTOS多任务（采集、显示、按键、Modbus），看门狗防止死机

6. 调试：串口打印系统状态（任务栈使用量、Modbus帧计数）

**第14周：框架搭建与任务划分**

- 🔴 **核心任务**：
  
  - 周一：画出系统架构图（任务、队列、互斥锁、共享资源）
  
  - 周二～周三：实现采集任务（周期2秒，发送温湿度+ADC到队列）
  
  - 周四：实现显示任务（接收队列，更新OLED）
  
  - 周五：实现按键任务（状态机，发送按键事件）
  
  - 周六～周日：缓冲

**第15周：Modbus集成与调试**

- 🔴 **核心任务**：
  
  - 周一：把Modbus从机独立成一个任务，监听RS485接收队列
  
  - 周二：Modbus任务读写共享的保持寄存器（需要用互斥锁）
  
  - 周三：让采集任务定期更新保持寄存器（地址0=温度,1=湿度,2=ADC）
  
  - 周四：用Modbus Poll进行长时间读测试（1小时）
  
  - 周五：增加看门狗，模拟死机（故意while(1)），验证复位
  
  - 周六～周日：缓冲

**第16周：完善、文档、演示**

- 🔴 **核心任务**：
  
  - 周一～周二：优化代码，增加注释和错误处理
  
  - 周三：录制演示视频（展示Modbus读写、按键切换显示、看门狗复位）
  
  - 周四：写项目说明文档（功能、硬件连接、软件架构、使用说明）
  
  - 周五：整理面试问答（如“Modbus帧间隔如何保证”、“优先级反转如何解决”）
  
  - 周六～周日：休息，准备投简历

**最终交付**：GitHub仓库链接 + 演示视频 + 简历上的项目描述。

---

## 关于“感觉自己笨”的特别说明

- **笨不是问题，放弃才是。** 你已经在夜班后坚持学习，这本身就说明你有很强的毅力。

- 这个计划中的每一个小里程碑（如“SPI驱动”、“Modbus从机”）都是可以独立验证的。每完成一个，你都会获得一次“我做到了”的正反馈。

- 如果在某周任务太难，你可以放慢节奏，用缓冲日消化。计划中的时间预留了20%的缓冲，完全够用。

- **建议你把每周的成果发到这个对话里**（哪怕只是“第X周完成”），我会根据你的反馈调整后续难度。

---

## 最后三个执行建议

1. **硬件采购清单**（总成本约50-80元）：
   
   - MAX485模块 × 2
   
   - W25Q64 Flash模块（或任意SPI设备）
   
   - 双绞线（网线剥开即可）
   
   - 如有条件，多买一块STM32F103最小系统板（作为RS485从机）

2. **环境优化**：
   
   - 安装Git，注册GitHub账号
   
   - 安装逻辑分析仪软件（如Saleae Logic）
   
   - 准备好Modbus Poll（免费试用版足够）

3. **心理建设**：
   
   - 把16周计划打印出来，每完成一周就划掉一周
   
   - 允许自己有“效率低的夜晚”，只要完成🔴核心任务就算胜利
   
   - 记住：你现在的每一行代码，都在拉近你与15k的距离
